{
  "name": "bird-thing",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create bird-detections",
    "db:init": "wrangler d1 execute bird-detections --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute bird-detections --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
